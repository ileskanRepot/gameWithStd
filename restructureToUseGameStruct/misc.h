#include <math.h>

#include "structs.h"

int mapInt(int inp, int input_start, int input_end, int output_start,
           int output_end);
double sign(double num);
double absMy(double num);
void swap(double *num0, double *num1);
double vecLen3d(struct Point3d *point3d);